{
  "name": "violate-price-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Smart bucket list price tracker for Violate.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
